<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidTech">

        <!-- 总目录 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 4.1 线性布局实验（通过 Intent Action 供 :demo 跳转，无直接依赖）-->
        <activity
            android:name=".chapter4.s1layout.Ch4S1LinearLabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="cn.edu.sziit.android.tech.lab.CH4_S1_LINEAR" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- 4.1 约束布局实验 -->
        <activity
            android:name=".chapter4.s1layout.Ch4S1ConstraintLabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="cn.edu.sziit.android.tech.lab.CH4_S1_CONSTRAINT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    </application>

</manifest>